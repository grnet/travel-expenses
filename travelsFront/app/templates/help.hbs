{{ page-toolbar pageTitle=(t "help.tab") }}

{{#paper-content classNames="md-padding"}}
  {{#if (can "see petition")}}
    {{partial 'traveler-manual'}}
  {{else if (can "approve petition")}}
    {{partial 'secretary-manual'}}
  {{else if (can "compensate petition")}}
    {{partial 'controller-manual'}}
  {{else if (can "managerApprove petition")}}
    {{partial 'manager-manual'}}
  {{else if (can "view petition")}}
    {{partial 'viewer-manual'}}
  {{else if (can "presidentApprove petition")}}
    {{partial 'president-manual'}}
  {{else}}
    {{partial 'user-manual'}}
  {{/if}}
{{/paper-content}}