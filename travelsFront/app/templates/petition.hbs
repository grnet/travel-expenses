{{ page-toolbar pageTitle=(if newPetition (t "petition_create.title") (t "petition_edit.title")) }}

{{#paper-content classNames="md-padding"}}
    {{ model-form/form model ui="user"}}
    {{#paper-button 
        onClick=(action "submitToState2")
        warn=true
        raised=true
        disabled=inProgress
    }}SUBMIT{{/paper-button}}
{{/paper-content}}
