{{#if newPetition}}
{{ page-toolbar pageTitle="Create a petition" }}
{{else}}
{{ page-toolbar pageTitle="Edit petition" }}
{{/if}}

{{#paper-content classNames="md-padding"}}
    {{ model-form/form model ui="user"}}
    {{#paper-button 
        onClick=submit 
        warn=true 
        raised=true
        disabled=inProgress
    }}SUBMIT{{/paper-button}}
{{/paper-content}}
