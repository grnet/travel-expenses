{{#if newPetition}}
	{{ page-toolbar pageTitle=(t "petition_create.title") }}
{{else}}
	{{ page-toolbar pageTitle=(t "petition_edit.title") }}
{{/if}}

{{#paper-content classNames="md-padding"}}
    {{ model-form/form model ui="user"}}
    {{#paper-button 
        onClick=(action "submitToState2")
        warn=true
        raised=true
        disabled=inProgress
    }}SUBMIT{{/paper-button}}
{{/paper-content}}
