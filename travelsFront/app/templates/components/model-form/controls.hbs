{{#paper-content classNames=""}}
    {{#paper-button 
        onClick=submit 
        accent=true
        raised=true
        disabled=inProgress
    }}{{ submitLabel }}{{/paper-button}}
{{#if showReset}}
    {{#paper-button onClick=reset accent=true raised=true}}RESET{{/paper-button}}
{{/if}}
{{ yield }}
{{/paper-content}}
