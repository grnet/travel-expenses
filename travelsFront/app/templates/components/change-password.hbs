{{gen-form
  passwordModel
  flat=true
  fieldsetFieldsComponent='paper-content'
  onSubmit=(action 'onSubmit')
  messages=(hash success='password.change.success')
  labels=(hash submit=(t 'password.change.submit'))
  modelMeta=(gen-resource-meta modelMeta)
}}

