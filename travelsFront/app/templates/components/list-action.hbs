{{#if iconButton}}
	{{#paper-button type="submit" raised=true onClick=(action "handleClick") iconButton=iconButton }}{{fa-icon icon}}{{/paper-button}}
{{else}}
	{{#paper-button type="submit" raised=true onClick=(action "handleClick") iconButton=iconButton }}{{value}}{{/paper-button}}
{{/if}}